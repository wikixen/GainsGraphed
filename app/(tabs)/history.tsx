import { StyleSheet, Text, View } from "react-native";

// History shows a pressable calendar; When a date is pressed it shows what exercises & foods a user did/ate; Also shows selfies if one was taken that day
const History = () => {
  return (
    <View>
      <Text>History</Text>
    </View>
  );
};

export default History;

const styles = StyleSheet.create({});
