import { StyleSheet, Text, View } from "react-native";
import React from "react";

// Current is shown when a user is actively exercising
const Current = () => {
  return (
    <View>
      <Text>CurrentScreen</Text>
    </View>
  );
};

export default Current;

const styles = StyleSheet.create({});
